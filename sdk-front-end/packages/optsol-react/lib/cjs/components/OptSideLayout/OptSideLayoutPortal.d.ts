import { PropsWithChildren } from 'react';
import { OptMenuSection } from '../OptDrawer';
interface Props {
    sections: OptMenuSection[];
}
export declare const OptSideLayoutPortal: ({ sections, children }: PropsWithChildren<Props>) => JSX.Element;
export {};
