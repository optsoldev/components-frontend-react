const ItemRegistros = () => {
  /*   const [todos, setTodos] = useState<Todo[]>([]);

  useEffect(() => {
    fetch("https://jsonplaceholder.typicode.com/todos")
      .then((response) => response.json())
      .then((response) => {
        console.log(response);
        return response;
      })
      .then(setTodos);
  }, []);

  console.log("item registros");

  return (
    <div>
      {todos.map((todo) => (
        <Box py={1}>
          <pre key={todo.id}>{JSON.stringify(todo)}</pre>
        </Box>
      ))}
    </div>
  ); */
  return <span>ItemRgistros</span>;
};

export default ItemRegistros;
